all_model_setups = [


    # ModelSetup(
    #     name="lesion_dsetection_with_fix_mobilenet",
    #     **lesion_detection_best_args,
    #     **mobilenet_args,
    #     **small_model_args,
    #     **common_args,
    #     **with_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="lesion_dsetection_baseline_mobilenet",
    #     **lesion_detection_best_args,
    #     **mobilenet_args,
    #     **small_model_args,
    #     **common_args,
    #     **without_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ##
    # ModelSetup(
    #     name="lesion_dsetection_with_fix_resnet",
    #     **lesion_detection_best_args,
    #     **resnet_args,
    #     **small_model_args,
    #     **common_args,
    #     **with_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="lesion_dsetection_baseline_resnet",
    #     **lesion_detection_best_args,
    #     **resnet_args,
    #     **small_model_args,
    #     **common_args,
    #     **without_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ##
    # ModelSetup(
    #     name="lesion_dsetection_with_fix_densenet",
    #     **lesion_detection_best_args,
    #     **densenet_args,
    #     **small_model_args,
    #     **common_args,
    #     **with_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="lesion_dsetection_baseline_densenet",
    #     **lesion_detection_best_args,
    #     **densenet_args,
    #     **small_model_args,
    #     **common_args,
    #     **without_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ##
    # ModelSetup(
    #     name="lesion_dsetection_with_fix_efficientnet_b0",
    #     **lesion_detection_best_args,
    #     **efficientnet_b0_args,
    #     **small_model_args,
    #     **common_args,
    #     **with_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="lesion_dsetection_baseline_efficientnet_b0",
    #     **lesion_detection_best_args,
    #     **efficientnet_b0_args,
    #     **small_model_args,
    #     **common_args,
    #     **without_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ##
    # ModelSetup(
    #     name="lesion_dsetection_with_fix_efficientnet_b5",
    #     **lesion_detection_best_args,
    #     **efficientnet_b5_args,
    #     **small_model_args,
    #     **common_args,
    #     **with_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="lesion_dsetection_baseline_efficientnet_b5",
    #     **lesion_detection_best_args,
    #     **efficientnet_b5_args,
    #     **small_model_args,
    #     **common_args,
    #     **without_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    ##
    # ModelSetup(
    #     name="lesion_dsetection_with_fix_vgg16",
    #     **lesion_detection_best_args,
    #     **vgg16_args,
    #     **small_model_args,
    #     **common_args,
    #     **with_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="lesion_dsetection_baseline_vgg16",
    #     **lesion_detection_best_args,
    #     **vgg16_args,
    #     **small_model_args,
    #     **common_args,
    #     **without_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    ##
    # ModelSetup(
    #     name="lesion_dsetection_with_fix_regnet_y_8gf",
    #     **lesion_detection_best_args,
    #     **regnet_y_8gf_args,
    #     **small_model_args,
    #     **common_args,
    #     **with_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="lesion_dsetection_baseline_regnet_y_8gf",
    #     **lesion_detection_best_args,
    #     **regnet_y_8gf_args,
    #     **small_model_args,
    #     **common_args,
    #     **without_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ##
    # ModelSetup(
    #     name="lesion_dsetection_with_fix_convnext_base",
    #     **lesion_detection_best_args,
    #     **convnext_base_args,
    #     **small_model_args,
    #     **common_args,
    #     **with_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="lesion_dsetection_baseline_convnext_base",
    #     **lesion_detection_best_args,
    #     **convnext_base_args,
    #     **small_model_args,
    #     **common_args,
    #     **without_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ##
    # ModelSetup(
    #     name="lesion_dsetection_with_fix_resnet50",
    #     **lesion_detection_best_args,
    #     **resnet50_args,
    #     **small_model_args,
    #     **common_args,
    #     **with_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="lesion_dsetection_baseline_resnet50",
    #     **lesion_detection_best_args,
    #     **resnet50_args,
    #     **small_model_args,
    #     **common_args,
    #     **without_fix_args,
    #     **reporting_report_args,  # see if disabling this will get more time.
    # ),
    ### fixation mode test ###
    ## the best model (in terms of test froc) is resnet50
    # ModelSetup(
    #     name="ld_with_fix_resnet50_silent",
    #     **lesion_detection_best_args,
    #     **resnet50_args,
    #     **small_model_args,
    #     **common_args,
    #     **with_fix_args,
    #     **silent_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="ld_with_fix_resnet50_full",
    #     **lesion_detection_best_args,
    #     **resnet50_args,
    #     **small_model_args,
    #     **common_args,
    #     **with_fix_args,
    #     **full_report_args,  # see if disabling this will get more time.
    # ),
    ### chepert and negbio performance.
    # ModelSetup(
    #     name="chexpert_resnet50_baseline",
    #     **chexpert_best_args,
    #     **resnet50_args,
    #     **small_model_args,
    #     **common_args,
    #     **without_fix_chexpert_args,
    #     **full_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="chexpert_resnet50_with_fix",
    #     **chexpert_best_args,
    #     **resnet50_args,
    #     **small_model_args,
    #     **common_args,
    #     **with_fix_chexpert_args,
    #     **full_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="negbio_resnet50_baseline",
    #     **negbio_best_args,
    #     **resnet50_args,
    #     **small_model_args,
    #     **common_args,
    #     **without_fix_negbio_args,
    #     **full_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="negbio_resnet50_with_fix",
    #     **negbio_best_args,
    #     **resnet50_args,
    #     **small_model_args,
    #     **common_args,
    #     **with_fix_negbio_args,
    #     **full_report_args,  # see if disabling this will get more time.
    # ),
    # ModelSetup(
    #     name="clinical_predictions",
    #     **clinical_best_args,
    #     **resnet50_args,
    #     **small_model_args,
    #     **common_args,
    #     **clinical_prediction_args,
    # ),
    # ModelSetup(
    #     name="clinical_predictions",
    #     **clinical_best_args,
    #     **resnet50_args,
    #     **small_model_args,
    #     **common_args,
    #     **gender_prediction_args,
    # ),
    # ModelSetup(
    #     name="age_predictions_norm",
    #     **age_prediction_best_r2,
    #     **resnet50_args,
    #     **small_model_args,
    #     **common_args,
    #     **age_prediction_args,
    # ),
]

# pick the best one.
